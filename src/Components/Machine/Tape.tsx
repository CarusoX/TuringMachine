import React from 'react';
import { Header } from 'semantic-ui-react';

function MachineTape(): React.ReactElement<typeof Header> {
  return <Header as="h2">This will be the tape</Header>;
}

export default MachineTape;
